services:
  - type: web
    name: smartecg-assist
    runtime: python
    plan: free
    buildCommand: pip install -r requirements-web.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT --timeout 120 --workers 1 web.app:app
    envVars:
      - key: PYTHON_VERSION
        value: "3.10.13"
